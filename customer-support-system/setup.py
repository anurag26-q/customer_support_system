from setuptools import find_packages,setup

setup(name='e-commerce-bot',
      version='0.0.1',
      author='anurag',
      packages=find_packages(),
      install_requrires=['langchain-astradb','langchain'])